export default {
    charactersList(state) {
        return state.characters;
    },
    pagesCount(state) {
        return state.pages;
    },
    rowsCount(state) {
        return state.rows;
    }
};
